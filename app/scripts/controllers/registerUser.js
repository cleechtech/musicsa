'use strict'

app.controller('RegisterUserCtrl', ['$scope', function($scope, $location){

	// holds all form data
	$scope.formData = {}

	$scope.addUser = function(){
		// create account

		// log user in

		// redirect to homepage
	}
}])